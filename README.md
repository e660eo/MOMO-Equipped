# MOMO — интернет-магазин автоакустики

Новый сайт для [momo-eq.ru](https://momo-eq.ru): Next.js + Tailwind CSS, деплой на Vercel.
Дизайн-направление — премиальный hi-end минимализм: **светлая тема по умолчанию
с переключателем на тёмную**, сигнальный оранжевый `#FF5500`, типографика
Unbounded / Manrope / JetBrains Mono, фирменный мотив — звуковая волна из логотипа.

## Статус

- [x] Данные каталога собраны с текущего сайта → `data/` (145 товаров, 6 категорий, 5 брендов)
- [x] **Фаза 1:** главная, каталог с фильтрами, карточка товара, корзина
      (оформление → WhatsApp), новости, контакты, реквизиты. Тема светлая/тёмная.
- [x] **Витрина доработана:** страница уценки `/sale`, фильтр цены и чипы фильтров,
      готовые сборки, рейтинг и отзывы, характеристики из названий товаров,
      живые подсказки в поиске, тосты корзины, прогресс до бесплатной доставки,
      маска телефона, плавающая кнопка WhatsApp, фирменная 404
- [x] **Оформление:** 3D-логотип на главной (левитация + доворот за курсором),
      тёмный футер с инфокартой, scroll-reveal секций
- [x] **Подготовка к публикации:** `sitemap.xml` (162 URL), `robots.txt`, favicon,
      OG-превью, репозиторий на GitHub, `DEPLOY.md` с инструкцией для VPS
- [x] **Каталог из прайса:** цены РРЦ, статус наличия и описания из Опт.Прайса,
      40 локальных фото товаров; фильтр «только в наличии» и сортировка
      «сначала в наличии»; мобильная вёрстка (фильтры в шторку, убран
      горизонтальный скролл)
- [x] **Юридический минимум (152-ФЗ):** страница `/privacy` — политика обработки
      персональных данных и текст согласия; чекбокс согласия обязателен в корзине,
      форме на контактах и при регистрации; ссылка в футере, страница в sitemap.
      **Текст — шаблон, его должен вычитать юрист;** отдельно нужно подать
      уведомление оператора ПД в Роскомнадзор
- [x] **SEO и аналитика:** разметка Schema.org (Product, BreadcrumbList,
      Organization, WebSite) в `src/lib/structured-data.ts`; Яндекс.Метрика
      (счётчик 110907002, Вебвизор + клик-карты) с ручным трекингом
      SPA-переходов — `src/components/yandex-metrica.tsx`
- [x] **Локальный личный кабинет (до серверной Фазы 3):** вход и регистрация
      по email/телефону + паролю — в браузере хранится только PBKDF2-хеш,
      пароль никуда не отправляется; профиль `/profile` с данными доставки и
      историей заказов этого устройства; заказ из корзины пишется в историю;
      кнопка «Войти через Яндекс ID» — честная заглушка «скоро» до переезда
      на сервер
- [x] **Отзывы очищены:** сгенерированные отзывы и рейтинги убраны с плиток
      каталога и карточки товара (`src/lib/reviews.ts` и `Stars` удалены —
      восстанавливаются из истории git, когда появятся настоящие). На месте
      секции — честное «отзывов пока нет» с приглашением оставить первый
- [ ] **Перед запуском обязательно:** статус наличия у остальных товаров,
      перенос оставшихся 105 фото со старого сайта
- [ ] Фаза 2: CMS — товары и цены редактируются без программиста
- [ ] Фаза 3: аккаунты, оформление заказа, оплата ЮKassa (нужны ключи магазина)
- [ ] Фаза 4: развёртывание на AdminVPS → проверка на запасном домене →
      переключение momo-eq.ru (DNS в панели Jino)

Репозиторий: <https://github.com/e660eo/MOMO-Equipped> (приватный, ветка `main`).

### Заметка про фото товаров

Источников два, `productImageUrl()` различает их по ведущему слэшу:

- **`/products/<slug>.webp`** — 40 фото, выгруженных из `Опт.Прайс МОМО 13.07.2026.xlsx`
  (динамики, сабвуферы, усилители, мультимедиа). Лежат локально в `public/products/`,
  обрезаны по краям и сжаты в webp: 34 МБ → 0,9 МБ.
- **имя файла без слэша** — остальные 105 товаров, грузятся со старого сайта
  (`momo-eq.ru/uploads/...`) через `ProductImage` с фолбэком на плейсхолдер.
  В песочнице разработки доступа к `momo-eq.ru` нет, поэтому в превью у них
  плейсхолдеры — **на вашей машине они отображаются**. Перед сменой DNS их нужно
  забрать скриптом `scripts/download-photos.mjs`.

Прежнее имя файла сохранено в поле `imageLegacy` — откат к фото со старого сайта
не требует повторного парсинга.

## Запуск (после установки Node.js)

```bash
npm install
npm run dev        # http://localhost:3000
```

## Структура

```
data/              # Seed-данные, собранные с живого momo-eq.ru
  products.json    #   145 товаров: slug, название, бренд, категория, цена, фото
  categories.json  #   6 категорий
  brands.json      #   MOMO, ZEUS, ANDROID, CAP, STARLIGHT
  news.json        #   3 новости
  site.json        #   контакты, реквизиты, база URL фото
design/prototype/  # Статичный HTML-прототип дизайна (утверждение стиля)
src/app/           # Next.js App Router: layout (шрифты), globals.css (токены)
src/components/ui/ # Компоненты shadcn-экосистемы: tubelight-navbar, spotlight-card/GlowCard,
                   #   web-gl-shader (звуковая линия), liquid-glass-button,
                   #   SpecularButton (ogl, эффект на кнопке «Каталог»), ClickSpark (искры по клику)

src/lib/           # Типы, слой данных, утилита cn() (позже данные — из Sanity)
```

## Дизайн-токены

Сайт стартует в светлой теме; тёмная включается кнопкой в шапке и запоминается
(в прототипе — `localStorage`, в приложении — так же + cookie для SSR).

| Токен     | Светлая (по умолчанию) | Тёмная    | Роль                                  |
| --------- | ---------------------- | --------- | ------------------------------------- |
| `bg`      | `#F7F5F2`              | `#0B0B0C` | фон                                   |
| `surface` | `#FFFFFF`              | `#17171A` | карточки, панели                      |
| `line`    | `#E5E1DA`              | `#232327` | линии, рамки                          |
| `fg`      | `#141416`              | `#F5F3EF` | текст                                 |
| `muted`   | `#6E6E73`              | `#8A8A8E` | вторичный текст                       |
| `signal`  | `#FF5500`              | `#FF5500` | акцент: CTA, цены, ховеры — только он |
| `tile`    | `#FFFFFF`              | `#F2EFEA` | плитки под фото товаров               |

Фото товаров пока хостятся на старом сайте (`site.json → imageBase`);
при переходе на CMS они переезжают в Sanity CDN.
