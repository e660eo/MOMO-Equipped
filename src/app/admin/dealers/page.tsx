import Link from "next/link";
import { Building2, FileCheck2, Package, UserPlus } from "lucide-react";
import { DealerCreateForm, type DealerCreateInitial } from "@/components/admin/dealer-create-form";
import { ConfirmButton } from "@/components/admin/confirm-button";
import { requireSession } from "@/lib/admin-auth";
import { b2bPriceCounts, DEALER_PRICE_TIER_LABELS, getB2BPriceBook } from "@/lib/b2b-prices";
import { dealerLocationKind, DEALER_LOCATION_KIND_LABELS } from "@/lib/dealer-location";
import { formatPrice } from "@/lib/format";
import { getDealerAccounts, getDealerApplications, getDealerLocation, getDealerLocations, getDealerOrders } from "@/lib/dealers";
import { deleteDealerAction, enableAndSendDealerAccess, resendDealerInvite, setDealerApplicationStatus, setDealerLocationProfileAction, setDealerLocationVisibility, setDealerOrderStatus, setDealerTerms } from "./actions";

const APP_LABELS = { new: "Новая", in_work: "В работе", approved: "Одобрена", rejected: "Отклонена" } as const;
const ORDER_LABELS = { new: "Новый", confirmed: "Подтверждён", shipped: "Отгружен", done: "Выполнен", canceled: "Отменён" } as const;
const BUSINESS_LABELS = { store: "Магазин", install: "Студия установки", online: "Интернет-магазин", mixed: "Несколько направлений" } as const;

export default async function AdminDealersPage({ searchParams }: { searchParams: Promise<{ application?: string }> }) {
  await requireSession();
  const applications = getDealerApplications();
  const accounts = getDealerAccounts();
  const locations = getDealerLocations(false);
  const orders = getDealerOrders();
  const priceBook = getB2BPriceBook();
  const priceCounts = b2bPriceCounts(priceBook);
  const params = await searchParams;
  const selected = applications.find((item) => item.id === params.application);
  const initial: DealerCreateInitial | undefined = selected ? { applicationId: selected.id, name: selected.company, city: selected.city, phone: selected.phone, contactName: selected.contactName, loginEmail: selected.email, website: selected.website } : undefined;
  const inputClass = "h-9 rounded-md border border-border bg-white px-2 text-xs";

  return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-signal">B2B</p><h1 className="mt-1 font-display text-3xl font-extrabold uppercase">Дилеры</h1><p className="mt-2 text-sm text-muted-foreground">Заявки, доступы, закрытые прайсы и дилерские заказы.</p>{priceBook.updatedAt && <p className="mt-2 text-xs text-muted-foreground">Прайсы обновлены {new Date(priceBook.updatedAt).toLocaleDateString("ru-RU")} · дилер {priceCounts.dealer} · Дагестан {priceCounts.dagestan} · опт {priceCounts.wholesale}</p>}</div><div className="flex gap-2 text-xs"><span className="rounded-full bg-signal/10 px-3 py-1.5 font-bold text-signal">{applications.filter((item) => item.status === "new").length} новых заявок</span><span className="rounded-full bg-black/5 px-3 py-1.5 font-bold">{orders.filter((item) => item.status === "new").length} новых заказов</span></div></div>
    <div className="mt-7 grid gap-5 lg:grid-cols-2"><section id="create" className="scroll-mt-24 rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-3"><UserPlus className="text-signal" size={21} /><div><h2 className="font-display text-lg font-extrabold uppercase">Создать дилера</h2><p className="text-xs text-muted-foreground">Назначьте прайс; скидка от РРЦ останется запасным правилом.</p></div></div>{selected && <p className="mt-4 rounded-lg bg-signal/8 px-3 py-2 text-xs text-signal">Форма заполнена из заявки «{selected.company}».</p>}<DealerCreateForm initial={initial} /></section>
      <section className="rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-3"><FileCheck2 className="text-signal" size={21} /><div><h2 className="font-display text-lg font-extrabold uppercase">Заявки</h2><p className="text-xs text-muted-foreground">{applications.length || "Пока нет"}</p></div></div><div className="mt-5 max-h-[660px] space-y-3 overflow-auto pr-1">{applications.map((application) => <article key={application.id} className="rounded-lg border border-border p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-signal">{BUSINESS_LABELS[application.businessType]}</p><h3 className="mt-1 font-bold">{application.company}</h3><p className="mt-1 text-xs text-muted-foreground">{application.city} · {application.contactName}</p><p className="mt-2 text-xs"><a className="hover:text-signal" href={`tel:${application.phone}`}>{application.phone}</a> · <a className="hover:text-signal" href={`mailto:${application.email}`}>{application.email}</a></p></div><span className="rounded-full bg-black/5 px-2 py-1 text-[10px] font-bold">{APP_LABELS[application.status]}</span></div>{application.comment && <p className="mt-3 rounded-md bg-black/[.025] p-2 text-xs leading-5 text-muted-foreground">{application.comment}</p>}<div className="mt-3 flex flex-wrap items-center gap-2"><Link href={`/admin/dealers?application=${application.id}#create`} className="rounded-md bg-signal px-3 py-2 text-xs font-bold text-white">Одобрить и создать</Link><form action={setDealerApplicationStatus} className="flex flex-1 gap-2"><input type="hidden" name="id" value={application.id} /><select className={`${inputClass} min-w-0 flex-1`} name="status" defaultValue={application.status}><option value="new">Новая</option><option value="in_work">В работе</option><option value="approved">Одобрена</option><option value="rejected">Отклонена</option></select><button className="rounded-md border border-border px-3 text-xs font-bold">Сохранить</button></form></div></article>)}{!applications.length && <p className="rounded-lg bg-black/[.025] p-6 text-center text-sm text-muted-foreground">Новые заявки со страницы «Стать дилером» появятся здесь.</p>}</div></section></div>

    <section className="mt-5 rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-3"><Building2 className="text-signal" size={21} /><div><h2 className="font-display text-lg font-extrabold uppercase">Дилерские точки</h2><p className="text-xs text-muted-foreground">Всего: {locations.length} · опубликовано: {locations.filter((item) => item.active).length} · скрыто: {locations.filter((item) => !item.active).length}</p></div></div><div className="mt-5 grid gap-3 lg:grid-cols-3">{locations.map((location) => { const account = accounts.find((item) => item.dealerId === location.id); const kind = dealerLocationKind(location); return <article key={location.id} className={`rounded-lg border p-4 ${location.active ? "border-border" : "border-dashed border-border bg-black/[.015]"}`}><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{location.name}</h3><p className="mt-1 text-xs text-muted-foreground">{location.city}, {location.address}</p><p className="mt-2 text-[10px] font-bold uppercase tracking-wider text-signal">{DEALER_LOCATION_KIND_LABELS[kind]}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${location.active ? "bg-emerald-50 text-emerald-700" : "bg-black/5 text-muted-foreground"}`}>{location.active ? "Опубликована" : "Скрыта"}</span></div><p className="mt-3 text-xs"><a className="hover:text-signal" href={`tel:${location.phone}`}>{location.phone}</a>{location.website && <> · <a className="font-semibold text-signal hover:underline" href={location.website} target="_blank" rel="noreferrer">Сайт ↗</a></>}</p><p className="mt-2 text-[11px] text-muted-foreground">{account ? `Кабинет: ${account.disabled ? "отключён" : "создан"}` : "Только публичная карточка — кабинет не выдавался"}</p><form action={setDealerLocationProfileAction} className="mt-4 grid gap-2"><input type="hidden" name="id" value={location.id} /><select className={inputClass} name="kind" defaultValue={kind}><option value="store">Магазин</option><option value="installation">Установочный центр</option><option value="store_install">Магазин / Установочный центр</option></select><label className="flex items-start gap-2 text-[11px] leading-4 text-muted-foreground"><input className="mt-0.5" type="checkbox" name="authorizedInstallation" defaultChecked={location.authorizedInstallation} />Установка с гарантией 24 месяца</label><button className="justify-self-start rounded-md border border-border px-3 py-2 text-xs font-bold hover:border-signal hover:text-signal">Сохранить тип</button></form><form action={setDealerLocationVisibility} className="mt-2"><input type="hidden" name="id" value={location.id} /><input type="hidden" name="active" value={String(!location.active)} /><button className={`rounded-md px-3 py-2 text-xs font-bold ${location.active ? "border border-border hover:border-red-300 hover:text-red-600" : "bg-signal text-white"}`}>{location.active ? "Скрыть из «Купить рядом»" : "Опубликовать"}</button></form><form action={deleteDealerAction} className="mt-2 border-t border-border pt-2"><input type="hidden" name="id" value={location.id} /><ConfirmButton label="Удалить дилера" question={`Удалить дилера «${location.name}»? Точка исчезнет с сайта${account ? ", а связанный дилерский кабинет будет удалён" : ""}. История заказов сохранится. Действие нельзя отменить.`} /></form></article>; })}</div></section>

    <section className="mt-5 rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-3"><Building2 className="text-signal" size={21} /><div><h2 className="font-display text-lg font-extrabold uppercase">Дилерские аккаунты</h2><p className="text-xs text-muted-foreground">{accounts.length} аккаунтов</p></div></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[980px] text-left text-xs"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Дилер</th><th className="pb-3">Вход</th><th className="pb-3">Состояние</th><th className="pb-3">Условия</th><th className="pb-3 text-right">Доступ</th></tr></thead><tbody className="divide-y divide-border">{accounts.map((account) => { const dealer = getDealerLocation(account.dealerId); const tier = account.priceTier ?? "dealer"; return <tr key={account.id}><td className="py-4"><b>{dealer?.name ?? "Точка удалена"}</b><p className="mt-1 text-muted-foreground">{dealer?.city} · {account.contactName}</p></td><td className="py-4">{account.email}<p className="mt-1 text-muted-foreground">{account.lastLoginAt ? `Был ${new Date(account.lastLoginAt).toLocaleDateString("ru-RU")}` : "Ещё не входил"}</p></td><td className="py-4"><span className={`rounded-full px-2 py-1 font-bold ${account.disabled ? "bg-red-50 text-red-700" : account.activatedAt ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}>{account.disabled ? "Отключён" : account.activatedAt ? "Активен" : "Ждёт активации"}</span></td><td className="py-4"><form action={setDealerTerms} className="flex items-center gap-2"><input type="hidden" name="accountId" value={account.id} /><select className={`${inputClass} w-40`} name="priceTier" defaultValue={tier}><option value="dealer">{DEALER_PRICE_TIER_LABELS.dealer}</option><option value="dagestan">{DEALER_PRICE_TIER_LABELS.dagestan}</option><option value="wholesale">{DEALER_PRICE_TIER_LABELS.wholesale}</option></select><input className={`${inputClass} w-16`} title="Резервная скидка" type="number" min="0" max="80" step="0.1" name="discountPercent" defaultValue={account.discountPercent} /><span>%</span><label className="flex items-center gap-1"><input type="checkbox" name="disabled" defaultChecked={account.disabled} /> выкл.</label><button className="rounded-md border border-border px-2 py-1.5 font-bold">OK</button></form></td><td className="py-4 text-right"><form action={resendDealerInvite}><input type="hidden" name="accountId" value={account.id} /><button className="font-bold text-signal hover:underline">Новая ссылка</button></form></td></tr>; })}</tbody></table></div></section>

    <section className="mt-5 rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-3"><Package className="text-signal" size={21} /><div><h2 className="font-display text-lg font-extrabold uppercase">Дилерские заказы</h2><p className="text-xs text-muted-foreground">Не смешиваются с розничными заказами.</p></div></div><div className="mt-5 space-y-3">{orders.map((order) => { const dealer = getDealerLocation(order.dealerId); return <article key={order.id} className="rounded-lg border border-border p-4"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-wider text-signal">{ORDER_LABELS[order.status]} · {new Date(order.createdAt).toLocaleString("ru-RU")}</p><h3 className="mt-1 font-bold">{order.id} · {dealer?.name ?? "Дилер"}</h3><p className="mt-1 text-xs text-muted-foreground">{order.items.map((item) => `${item.title} × ${item.qty}`).join("; ")}</p>{order.comment && <p className="mt-2 text-xs">Комментарий: {order.comment}</p>}</div><div className="text-right"><p className="text-lg font-black">{formatPrice(order.total)}</p><form action={setDealerOrderStatus} className="mt-2 flex gap-2"><input type="hidden" name="id" value={order.id} /><select className={inputClass} name="status" defaultValue={order.status}><option value="new">Новый</option><option value="confirmed">Подтверждён</option><option value="shipped">Отгружен</option><option value="done">Выполнен</option><option value="canceled">Отменён</option></select><button className="rounded-md bg-black px-3 text-xs font-bold text-white">Сохранить</button></form></div></div></article>; })}{!orders.length && <p className="rounded-lg bg-black/[.025] p-6 text-center text-sm text-muted-foreground">Дилерские заказы появятся после отправки из кабинета.</p>}</div></section>
    <section className="mt-5 rounded-xl border border-border bg-surface p-5">
      <h2 className="font-display text-lg font-extrabold uppercase">Управление доступом</h2>
      <p className="mt-1 text-xs text-muted-foreground">Включение аккаунта и безопасная ссылка для установки нового пароля.</p>
      <div className="mt-4 grid gap-3 lg:grid-cols-2">
        {accounts.map((account) => {
          const dealer = getDealerLocation(account.dealerId);
          return <article key={`access-${account.id}`} className="rounded-lg border border-border p-4">
            <div className="flex flex-wrap items-start justify-between gap-3">
              <div>
                <p className="font-bold">{dealer?.name ?? account.contactName}</p>
                <p className="mt-1 text-xs text-muted-foreground">{account.email}</p>
                <p className={`mt-2 text-xs font-semibold ${account.disabled ? "text-red-600" : account.activatedAt ? "text-emerald-700" : "text-amber-700"}`}>
                  {account.disabled ? "Доступ отключён" : account.activatedAt ? "Доступ активен" : "Ожидает установки пароля"}
                </p>
                {account.lastAccessMailAt && <p className={`mt-1 text-[11px] ${account.lastAccessMailError ? "text-red-600" : "text-muted-foreground"}`}>
                  {account.lastAccessMailError ? `Письмо не отправлено: ${account.lastAccessMailError}` : `Последняя ссылка отправлена ${new Date(account.lastAccessMailAt).toLocaleString("ru-RU")}`}
                </p>}
              </div>
              <form action={enableAndSendDealerAccess}>
                <input type="hidden" name="accountId" value={account.id} />
                <button className="rounded-md bg-signal px-3 py-2 text-xs font-bold text-white">
                  {account.disabled ? "Включить и отправить" : account.activatedAt ? "Новый пароль" : "Активировать"}
                </button>
              </form>
            </div>
          </article>;
        })}
      </div>
    </section>
  </div>;
}
